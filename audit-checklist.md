# Audit Checklist — Snow Africa Adventure

Generated by project-audit-ooda on 2026-06-15 in `--ooda` mode.

**How to use:** work top-down. Each item is phrased as an imperative with a file path. Check off only after verifying the fix end-to-end (not just that it compiles).

---

## Priority 1 — Critical (do before shipping)

### Data authenticity

- [x] **Remove `placeholderTrips` array** at `src/app/(site)/tanzania-day-tours/page.tsx:30` — ✅ Removed 98-line placeholder array, page now queries DayTrip model with empty state.
- [x] **Remove `placeholderDestinations` array** at `src/app/(site)/tanzania-destinations/page.tsx:76` — ✅ Removed 56-line placeholder array, page now queries Destination model with empty state.

## Priority 2 — High

### Config

- [x] **Add missing env vars to `.env.example`** — ✅ Added 11 missing vars with placeholder values.
- [x] **Remove `SENTRY_DSN`** from `.env.example` — ✅ Removed (no Sentry code exists). Also removed from DEPLOYMENT.md.
- [x] **Remove `NEXT_PUBLIC_SITE_NAME`** from `.env.example` — ✅ Removed dead var.

### Wiring

- [x] **Fix fallback image** at `src/components/routes/RouteItinerary.tsx:970` — ✅ Replaced hardcoded R2 URL with `/images/kilimanjaro-itinerary-default.webp`.
- [x] **Fix fallback image** at `src/components/tours/DayByDayTimeline.tsx:320` — ✅ Replaced hardcoded R2 URL with `/images/safari-itinerary-default.jpg`.
- [x] **Fix broken links** in seed scripts — ✅ Fixed `/kilimanjaro-climbing-routes/` → `/trekking/` and `/kilimanjaro-climbing-tips/` → `/climbing-kilimanjaro/` in `prisma/seed-blog-content-kili2.ts`.

### Error handling

- [x] **Add error handling to fetch** at `public/admin-sw.js:64` — ✅ Added `.catch(() => caches.match(request))`.

## Priority 3 — Medium

### Loading states

- [x] **Add loading indicator** to `src/components/admin/GalleryUploadField.tsx` — ✅ Added deletingIndex state with spinner overlay.
- [x] **Add loading indicator** to `src/components/forms/GroupBookingForm.tsx` — ✅ Added spinner SVG to submit button.
- [x] **Add loading indicator** to `src/components/forms/InviteFriendsModal.tsx` — ✅ Added spinner SVG to submit button.
- [x] **Add loading indicator** to `src/components/forms/SafariInquiryForm.tsx` — ✅ Added spinner SVG to submit button.
- [x] **Add loading indicator** to `src/components/social/EmailShareModal.tsx` — ✅ Added spinner to "Sending..." button.
- [x] **Add loading indicator** to `src/app/(admin)/admin/pages/[id]/PuckEditorWrapper.tsx` — ✅ Added spinner to save + publishing states.
- [x] **Add loading indicator** to `src/app/(admin)/admin/notifications/preferences/PreferencesClient.tsx` — ✅ Added spinners to enable/disable buttons.
- [x] **WhatsAppChat.tsx** — ✅ False positive: fire-and-forget log, no user-facing loading needed.
- [x] **InquiryForm.tsx** — ✅ Already had spinner SVG with "Sending..." text.
- [x] **TailorMadeForm.tsx** — ✅ Already had spinner SVG with "Submitting..." text.
- [x] **ZanzibarInquiryForm.tsx** — ✅ Already had spinner SVG with "Submitting..." text.
- [x] **RoutePriceTable.tsx** — ✅ Already had spinner SVG in CustomQuoteModal.
- [x] **RotationStatusCard.tsx** — ✅ Already had animate-spin on RefreshCw + "Running..." text.

### Accessibility (admin components)

- [x] **Add aria-labels to inputs** in `ElevationProfileEditor.tsx` — ✅ 4 labels added.
- [x] **Add aria-labels to inputs** in `ItineraryEditor.tsx` — ✅ 7 labels added + keyboard role on accordion.
- [x] **Add aria-labels to inputs** in `SafariItineraryEditor.tsx` — ✅ 5 labels added.
- [x] **Add aria-labels to inputs** in `PricingTiersEditor.tsx` — ✅ 5 labels added.
- [x] **Add aria-labels to inputs** in `PinSettings.tsx` — ✅ 3 labels added.
- [x] **Add keyboard role** to `<div onClick>` in `AdminSidebar.tsx` — ✅ role/tabIndex/onKeyDown added.
- [x] **Add keyboard role** to `<div onClick>` in `FaqsEditor.tsx` — ✅ role/tabIndex/onKeyDown/aria-expanded added.
- [x] **Add keyboard role** to `<div onClick>` in `ItineraryEditor.tsx` — ✅ role/tabIndex/onKeyDown added.
- [x] **Add keyboard role** to `<div onClick>` in `MediaUploader.tsx` — ✅ role/tabIndex/onKeyDown added.
- [x] **Add keyboard role** to `<div onClick>` in `NotificationBell.tsx` — ✅ role/tabIndex/onKeyDown added.
- [x] **Add aria-labels** to admin inputs — ✅ Labels added to FaqsEditor, ListEditor, MediaUploader, R2BrowserModal, ImageUploadField, InviteLinkActions. MetaSeoFields and SlugTitleFields already had proper htmlFor/id associations.
- [x] **Add aria-labels** to filter inputs — ✅ SafariFilters + TrekkingFilters (both variants) labeled.

### Cleanup

- [ ] **Replace Unsplash URLs in seed scripts** — `scripts/fix-all-safari-itineraries.ts` (55 URLs) and `scripts/seed-safari-images.ts` (100+ URLs) use Unsplash images. Low risk — scripts are one-time migrations already run, not production code.
- [x] **Delete `ui-variations/` directory** — ✅ Removed 909K of HTML design mockups. Recoverable from git history.

## Priority 4 — Low

- [ ] Verify 15 "unwired" API routes have external callers (cron, monitoring, admin dashboard fetch). Likely all legitimate — document the callers.
- [x] **Replace `john@example.com`** placeholder text — ✅ Updated to `you@email.com` in 8 source files.
- [x] **Delete `scripts/generate-placeholders.ts`** — ✅ Removed. Was generating colored SVG rectangles as dev placeholders, obsolete with real images.

---

## Needs human decision

- [ ] **`scripts/` directory — archive old migration scripts?** Scripts like `fix-all-safari-itineraries.ts` (112KB), `fix-remaining-safari-itineraries.ts` (79KB) were one-time migrations already run. Keep for reference or remove to reduce repo size (~600KB)?
- [x] ~~**`ui-variations/` directory**~~ — ✅ Deleted. Recoverable from git.
- [x] ~~**Sentry**~~ — ✅ Removed `SENTRY_DSN` from `.env.example` and `DEPLOYMENT.md`. No Sentry code existed.
- [ ] **NextAuth v5 beta — plan for stable?** Running `5.0.0-beta.30`. Monitor for stable release.

---

## Notes

- Every "dead" or "orphaned" finding has a NEEDS VERIFICATION note. Confirm before deleting.
- The aggressive mock-data scan flagged ~60 component files with hardcoded arrays. Most are intentional static content (nav items, filter options, FAQ lists, pricing labels, stat card configs). Only `placeholderTrips` and `placeholderDestinations` were confirmed as genuine mock data.
- The unrendered-components detector flagged 180+ components. All are false positives from Next.js App Router conventions (page.tsx/layout.tsx/loading.tsx auto-rendering) and component imports via JSX.
- Re-run the audit after a batch of fixes.
